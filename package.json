{
  "name": "koopcare-fullstack-demo-platform",
  "version": "0.3.0",
  "private": true,
  "description": "Independent fullstack demo platform for KoopCare user and admin financing workflows with ML credit scoring integration.",
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace @koopcare-demo/api\" \"npm run dev --workspace @koopcare-demo/web\"",
    "dev:api": "npm run dev --workspace @koopcare-demo/api",
    "dev:web": "npm run dev --workspace @koopcare-demo/web",
    "build": "npm run build --workspace @koopcare-demo/api && npm run build --workspace @koopcare-demo/web",
    "check": "npm run typecheck --workspace @koopcare-demo/api && npm run typecheck --workspace @koopcare-demo/web && npm run build",
    "check:deploy-config": "node scripts/deployment-config-check.mjs",
    "smoke:api": "npm run build --workspace @koopcare-demo/api && node scripts/api-smoke-check.mjs",
    "smoke:public": "npm run build && node scripts/public-preview-smoke-check.mjs",
    "start": "node scripts/start-public-demo.mjs",
    "preview:public": "npm run build && node scripts/start-public-demo.mjs",
    "start:api": "npm run start --workspace @koopcare-demo/api"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
