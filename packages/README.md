# Packages

This directory is reserved for shared packages if the project needs them later.

Potential shared packages:

- shared TypeScript types;
- shared validation schemas;
- shared constants;
- shared UI tokens.

No shared package is required in Progress 01.

